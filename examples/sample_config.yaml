workspace_dir: ./.cuos_workspace
parser:
  default: mock
  adapters:
    mock: {}
llm:
  provider: mock
  model: mock-model
  api_key_env: DASHSCOPE_API_KEY
  enable_thinking: false
  timeout: 120
prompts:
  dir: ./prompts
